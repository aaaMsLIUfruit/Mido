server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mido?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8&useUnicode=true
    username: root
    password: "050612"
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    map-underscore-to-camel-case: true

logging:
  level:
    com.mido.backend: debug
    com.baomidou: debug

cors:
  allowed-origins:
    - http://localhost:4173
    - http://localhost:5173

app:
  upload:
    root-dir: uploads
    base-url: http://localhost:8080/uploads

ai:
  deepseek:
    base-url: https://api.deepseek.com
    chat-path: /chat/completions
    model: deepseek-chat
    temperature: 0.6
    api-key: sk-1d5ee38356264df8bcd071ebc586db8d

security:
  jwt:
    secret: ${JWT_SECRET:change-me-change-me-change-me-change-123456}
    expiration-minutes: 1440